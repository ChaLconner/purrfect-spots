{
  "version": 2,
  "framework": null,
  "installCommand": "uv pip install -r requirements.txt --system",
  "buildCommand": "echo 'No build step required for Python backend'",

  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api/index.py"
    }
  ]
}