# ===========================================
# PROJECT ENVIRONMENT VARIABLES EXAMPLE
# ===========================================
# This is a comprehensive example file for the entire project
# Copy relevant sections to appropriate .env files:
# - frontend/.env (for development)
# - frontend/.env.production (for production)
# - backend/.env (for backend)

# ===========================================
# FRONTEND VARIABLES
# ===========================================
# Google OAuth Configuration (Frontend)
# Note: Frontend variables need VITE_ prefix for Vite
VITE_GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com
VITE_GOOGLE_CLIENT_ID_SECRET=your_google_client_secret

# API Configuration (Frontend)
VITE_API_BASE_URL=http://localhost:8000

# Google Maps API Key (Frontend)
VITE_GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here

# Frontend Configuration
VITE_DEBUG_MODE=true
VITE_LOG_LEVEL=debug

# ===========================================
# BACKEND VARIABLES
# ===========================================
# Google OAuth Configuration (Backend)
GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your_google_client_secret

# JWT Configuration (Backend)
JWT_SECRET=your_jwt_secret_key_here

# Database Configuration (Backend)
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_KEY=your_supabase_anon_key_here
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here
DATABASE_URL=postgresql://user:password@host:port/database

# AWS Configuration (Backend)
AWS_ACCESS_KEY_ID=your_aws_access_key_here
AWS_SECRET_ACCESS_KEY=your_aws_secret_key_here
AWS_REGION=ap-southeast-2
AWS_S3_BUCKET=your_s3_bucket_name

# Google AI Configuration (Backend)
GOOGLE_AI_API_KEY=your_google_ai_api_key_here
GOOGLE_VISION_KEY_PATH=backend/keys/google_vision.json

# CORS Configuration (Backend)
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# Server Configuration (Backend)
DEBUG=False
PORT=8000
ENVIRONMENT=development

# ===========================================
# ENVIRONMENT SETUP INSTRUCTIONS
# ===========================================
# 1. FRONTEND SETUP:
#    - Copy frontend variables to frontend/.env for development
#    - Copy frontend variables to frontend/.env.production for production
#    - Replace placeholder values with actual credentials

# 2. BACKEND SETUP:
#    - Copy backend variables to backend/.env
#    - Replace placeholder values with actual credentials
#    - Ensure service account files are properly placed

# 3. DEVELOPMENT VS PRODUCTION:
#    - Development: Use local URLs, enable debug mode
#    - Production: Use production URLs, disable debug mode

# ===========================================
# SECURITY BEST PRACTICES
# ===========================================
# 1. Never commit actual .env files to version control
# 2. Use different credentials for development and production
# 3. Regularly rotate secrets and API keys
# 4. Restrict API keys to specific domains/IPs when possible
# 5. Keep sensitive data on the backend only
# 6. Use HTTPS in production
# 7. Monitor API usage for unusual activity