# API Configuration
# The application now automatically detects the environment:
# - Local development (localhost): Uses http://localhost:8000
# - Vercel deployment: Uses /api (relative path for same-origin requests)
# - Manual override: Uncomment and set your preferred URL below

# Manual override (optional - only set if you need to override auto-detection)
# VITE_API_BASE_URL=http://localhost:8000
# VITE_API_BASE_URL=https://purrfect-spots-backend.vercel.app

# Instructions:
# - Development: Leave commented out for auto-detection, or set to localhost:8000
# - Production: Leave commented out for auto-detection, or set to your backend URL
# - The app will automatically work in both local and Vercel environments

# Google Maps API Key
VITE_GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here

# CDN Configuration (for production)
# Uncomment and set this when using a CDN for static assets
# VITE_CDN_BASE_URL=https://your-cdn-domain.com/assets

# Image Optimization Settings
# Maximum image width for optimization (in pixels)
VITE_MAX_IMAGE_WIDTH=1920

# Maximum image height for optimization (in pixels)
VITE_MAX_IMAGE_HEIGHT=1080

# Image quality for optimization (0-100)
VITE_IMAGE_QUALITY=85

# Maximum file size for upload (in MB)
VITE_MAX_FILE_SIZE=10