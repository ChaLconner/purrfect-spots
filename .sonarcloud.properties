# Keep SonarCloud Automatic Analysis focused on application source and tests.
# Database migrations are deployment artifacts and are validated by Supabase
# migration checks rather than application-code quality rules.
sonar.sources=backend,frontend/src
sonar.tests=backend/tests,frontend/tests
sonar.exclusions=supabase/migrations/**,backend/migrations/**
